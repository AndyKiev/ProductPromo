# Never copy the host's Windows node_modules into the Linux image — native
# modules (esbuild, rollup) are platform-specific and will not run.
**/node_modules/
dist/
dist-ssr/
.vite/
.tanstack/

.env
.env.local
.env.*.local

*.log
