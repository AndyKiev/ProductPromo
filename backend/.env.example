# Copy to .env and fill in. All keys are prefixed APP_CONFIG__ with __ nesting.

# --- run ---
APP_CONFIG__RUN__HOST=0.0.0.0
APP_CONFIG__RUN__PORT=8004
APP_CONFIG__RUN__RELOAD=true

# --- legacy MySQL (existing DB; no schema changes) ---
APP_CONFIG__DB__HOST=host.docker.internal
APP_CONFIG__DB__PORT=3306
APP_CONFIG__DB__USER=readonly_user
APP_CONFIG__DB__PASSWORD=change_me
APP_CONFIG__DB__NAME=aula
APP_CONFIG__DB__ECHO=false

# --- CORS (dev: open) ---
# APP_CONFIG__CORS__ORIGINS=["http://localhost:4000"]
